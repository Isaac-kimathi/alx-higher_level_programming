#!/usr/bin/python3

def magic_calculation(a, b):

    from magic_calculation_102 import add, sub

    if x < y:
        z = add(x, y)
        for q in range(4, 6):
            z = add(z, q)
        return (z)

    else:
        return (sub(x, y))
